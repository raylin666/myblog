export interface CommonSettings {
  iconUrl: string
  logoUrl: string
  authorName: string
  authorDescription: string
  avatarUrl: string
  topAvatarUrl: string
  siteTitle: string
  coverUrls: string[]
}